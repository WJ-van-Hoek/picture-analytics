# Picture Analytics

Refactor of the single-file JPG parsing tool into a small package of classes.

## Install
```bash
pip install -r requirements.txt
```

## Run
```bash
python cli.py -i path/to/image.jpg -o out --ocr --ocr-lang nld+eng --max-dim 1600 --format png
```
